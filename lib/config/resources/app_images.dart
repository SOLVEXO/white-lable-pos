class AppImages {
  static const String _baseImagePath = "assets/images/";

  /// This app's logo — always this bundled asset, never a network URL.
  static const String logoImage = '${_baseImagePath}logo.png';

  static const String placeHolderImage = '${_baseImagePath}default_image_placeholder.png';
}
